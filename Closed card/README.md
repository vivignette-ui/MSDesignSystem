
  # Closed map component

  This is a code bundle for Closed map component. The original project is available at https://www.figma.com/design/Umw3SeppFI1Dv9xD6ub7eh/Closed-map-component.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  